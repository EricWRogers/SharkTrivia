using UnityEngine;
using TMPro;
using System.Collections.Generic;

public class UnassignRename : MonoBehaviour
{
    public char englishText;
    public char cipherText;
    public TMP_Text unassTitleText;
    public GameObject unassButton;

    //take in english char value of button pressed (englishText)
    //take in current assignment from cipher decode (cipherText)
    //assign that cipherText value to unassButton
    //change unassTitleText to include both cipherText and englishText values with cipherText using the other font
    //when unassButton is pressed: assign that cipherText value to the CipherDecode.instance key that matches englishText


}
